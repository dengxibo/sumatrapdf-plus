# SumatraPDF Plus 3.7.23 (2026-08-04)

**Download:** [SumatraPDF.exe](https://github.com/dengxibo/sumatrapdf-plus/releases/download/3.7.23/SumatraPDF.exe) (portable x64)

Full release page: [docs/releases/3.7.23.md](https://github.com/dengxibo/sumatrapdf-plus/blob/main/docs/releases/3.7.23.md)

---

### What's new

**PDF smart dark mode** — fix inverted colors on RAZ picture-book pages by classifying large images as photo/illustration vs scanned page background.

**Embedded PDF audio** — play Sound, RichMedia, and Screen annotations (e.g. RAZ speaker icons) from embedded MP3 assets in the PDF; no network required.

**Faster tab close and switch** — detach the document and delete the engine on a background thread instead of blocking on slow synchronous teardown.

**Faster application exit** — abort background reflow/loading and skip synchronous per-page engine teardown on quit (especially while large ebooks are still loading).

### 本版更新

**PDF 智能暗色模式** — 修复 RAZ 绘本页面颜色反转：区分插图/照片与扫描页背景，插图保持正常色彩。

**PDF 内嵌音频** — 支持播放 Sound、RichMedia、Screen 注释（如 RAZ 扬声器图标），从 PDF Assets 读取 MP3，无需联网。

**加快关闭与切换标签** — 异步释放文档引擎，关闭标签不再等待耗时的同步析构。

**加快退出程序** — 退出时中止后台重排/加载，跳过逐页同步释放（大电子书仍在加载时尤其明显）。

### Upgrade

Exit SumatraPDF completely before replacing the portable executable.

### 升级说明

便携版请先完全退出，再替换程序。
